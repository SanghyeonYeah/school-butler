name: routine_partner
description: 하루의 루틴을 대신 기억해주고, 캐릭터와 함께 계획·실행·회고까지 동행하는 일정관리 파트너 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 상태 관리
  provider: ^6.1.1
  
  # 날짜 처리
  intl: ^0.20.2
  
  # UUID 생성
  uuid: ^4.2.1
  
  # 아이콘
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
